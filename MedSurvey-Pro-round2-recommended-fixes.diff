diff --git a/routes/web.php b/routes/web.php
--- a/routes/web.php
+++ b/routes/web.php
@@ -38,8 +38,8 @@ Route::middleware(['auth', 'audit.mutations'])->prefix('dashboard')->name('dashbo
     Route::get('/', [DashboardController::class, 'index'])->name('index')->middleware('can:dashboard.view');
     Route::post('/change-password', [AccountController::class, 'changePassword'])->name('change-password');
     Route::get('/responses', [ResponseController::class, 'responses'])->name('responses')->middleware('can:responses.view');
-    Route::get('/responses/filter', [ResponseController::class, 'filterResponses'])->name('responses.filter');
-    Route::get('/responses/{id}/json', [ResponseController::class, 'showResponseJson'])->name('responses.json');
+    Route::get('/responses/filter', [ResponseController::class, 'filterResponses'])->name('responses.filter')->middleware('can:responses.view');
+    Route::get('/responses/{id}/json', [ResponseController::class, 'showResponseJson'])->name('responses.json')->middleware('can:responses.view');
 
     Route::get('/kiosk/enter', [DashboardController::class, 'enterKioskMode'])->name('kiosk.enter');
     Route::get('/kiosk/exit', [DashboardController::class, 'exitKioskMode'])->name('kiosk.exit');
diff --git a/database/seeders/DatabaseSeeder.php b/database/seeders/DatabaseSeeder.php
--- a/database/seeders/DatabaseSeeder.php
+++ b/database/seeders/DatabaseSeeder.php
@@ -16,6 +16,8 @@ class DatabaseSeeder extends Seeder
             throw new \RuntimeException('SUPER_ADMIN_PASSWORD must be set to a strong password in production.');
         }
 
+        $this->call(RolesAndPermissionsSeeder::class);
+
         User::query()->updateOrCreate(
             ['username' => env('SUPER_ADMIN_USERNAME', 'admin')],
             [
diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -57,6 +57,9 @@ jobs:
       - name: Run migrations
         run: php artisan migrate -q --no-ansi --no-interaction --env=testing
 
+      - name: Seed roles and permissions
+        run: php artisan db:seed --class=Database\\Seeders\\RolesAndPermissionsSeeder --no-ansi --no-interaction --env=testing
+
       - name: Run Pint
         run: composer run pint:test
 
@@ -152,6 +155,9 @@ jobs:
       - name: Run migrations
         run: php artisan migrate -q --no-ansi --no-interaction --env=testing
 
+      - name: Seed roles and permissions
+        run: php artisan db:seed --class=Database\\Seeders\\RolesAndPermissionsSeeder --no-ansi --no-interaction --env=testing
+
       - name: Install Playwright Chromium
         run: npx playwright install --with-deps chromium
 
diff --git a/resources/views/dashboard/responses.blade.php b/resources/views/dashboard/responses.blade.php
--- a/resources/views/dashboard/responses.blade.php
+++ b/resources/views/dashboard/responses.blade.php
@@ -905,7 +905,7 @@
             return `
               <span class="${this.ratingBadgeClass(numericValue, scale)}">
                 ${starsHtml}
-                <span>${scale} / ${numericValue}</span>
+                <span>${numericValue} / ${scale}</span>
               </span>
             `;
           }
diff --git a/resources/views/dashboard/tickets.blade.php b/resources/views/dashboard/tickets.blade.php
--- a/resources/views/dashboard/tickets.blade.php
+++ b/resources/views/dashboard/tickets.blade.php
@@ -757,7 +757,7 @@
             return `
               <span class="${this.ratingBadgeClass(numericValue, scale)}">
                 ${starsHtml}
-                <span>${scale} / ${numericValue}</span>
+                <span>${numericValue} / ${scale}</span>
               </span>
             `;
           }
